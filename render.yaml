databases:
  - name: my_fullcourse
    databaseName: my_fullcourse_production
    user: my_fullcourse
    region: singapore

services:
  - type: web
    name: my_fullcourse
    env: ruby
    region: singapore
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: my_fullcourse_production
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false